

import {
    panggil
} from './data1.js';

for (let i = 0; i < panggil.nama.length; i++) {
    console.log("NAMA: " + panggil.nama[i]);
    console.log("NIM: " + panggil.nim[i]);
    console.log(" ");
}